<?php
return array (
    'invokables' => array(
        /*
         * Имя плагина должно совпадать с методом, к которому будет обащение как к плагину ($this->currentDate())
         * <имя плагина> => <путь к плагину>
         */
		'currentDate' => 'Plugins\Plugin\CurrentDate',
    )
);

